[project]
name = "dbviz"
version = "0.1.4.dev2"
description = "Decision Boundary Visualization for PyTorch Models"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib",
    "numpy",
    "torch",
    "torchvision",
]

[dependency-groups]
dev = [
    "ruff",
]
